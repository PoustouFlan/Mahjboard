services:
  mahjboard:
    build: .
    container_name: mahjboard
    restart: always
    ports:
      - 5000:5000
    volumes:
      - ./mahjong_data:/app/instance
    environment:
      - SCRIPT_NAME=/mahjong
networks: {}
